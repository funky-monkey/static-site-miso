---
title: "Welcome to your new Miso site"
layout: "page.twig.html"
---

# Getting started

Replace this Markdown file with your own content. Add more Markdown files inside the `content/`
directory, or create sub-directories to build collections (for example `content/blog/`).

Run `miso build` to compile the site into the `_site/` directory.
